#!/bin/bash
Specifies that the script should be executed using the Bash shell.

if [ "$#" -ne 2 ]; then
Ensures exactly two command-line arguments (dirA and dirB) are provided.

dirA="$1", dirB="$2"
Stores the directory paths passed as arguments.

if [ ! -d "$dirA" ] || [ ! -d "$dirB" ]; then
Checks whether both arguments are valid directories.

ls "$dirA" | sort and ls "$dirB" | sort
Lists and sorts file names from both directories.

comm -23
Displays files only present in dirA.

comm -13
Displays files only present in dirB.

comm -12
Extracts files common to both directories.

cmp -s file1 file2

Compares file contents silently

Returns success if files are identical

echo "$file : Contents are SAME/DIFFERENT"
Prints comparison result for each common file.
